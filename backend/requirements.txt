# Tark backend — Phase 0
# Lower-bound pins so pip resolves the newest wheels (important on Python 3.14).

# Web framework + server
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.10
pydantic-settings>=2.6
python-dotenv>=1.0

# Model providers
google-genai>=1.0          # Gemini — unified SDK: `from google import genai`
openai>=1.55               # NVIDIA NIM is OpenAI-compatible

# Auth: JWT session tokens (password hashing + email use the stdlib)
PyJWT>=2.9

# Correctness engine (CLAUDE.md §7)
sympy>=1.13

# RAG: curriculum retrieval (PDF parsing + local embeddings + vector math)
pymupdf>=1.24
numpy>=1.26
fastembed>=0.3          # local ONNX embeddings (no API limits, no PyTorch)

# Database (CLAUDE.md §5)
supabase>=2.10
